var numeros, pares, impares;
var j=0;var z=0;
for(var i=0;i<50;i++){
	numeros[i]=Math.floor((Math.random() * 100) + 1);
	if numeros[i]%2==0{
		pares[j]=numeros[i];
		j++;
		}
		else{
			impares[z]=numeros[i];
			z++;
		}
}

